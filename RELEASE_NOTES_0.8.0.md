# PodPlayer v0.8.0

## 本版重点

- **临时音频转写**：对未永久下载单集手动生成文字稿，或明确选择“生成文字稿并总结”时，PodPlayer 会仅为当前任务准备临时音频。成功后保留文字稿和总结并清理临时媒体；暂停和可重试网络失败可在有限时间内续转，不会进入下载列表、NAS 托管或备份。
- **任务生命周期收口**：转写任务明确区分暂停、取消、可重试失败、永久失败和关闭；永久下载进行中时优先等待并复用其产物，避免同一单集出现重复音频请求。
- **封面外拖偏好**：设置页新增“允许拖出播客封面”，默认开启且立即生效。它只影响播客/单集封面；Shownotes 图片和播放队列的拖拽手柄保持原行为。
- **设置页说明**：NAS 连接旁新增可键盘访问的 Audiobookshelf 帮助说明；睡眠关机、本地转写和下载并发说明改为事实化表述。
- **运行时一致性**：补强快捷键默认项迁移、ASR 子进程错误结算、详情页异步结果防陈旧回写、订阅刷新失败提示过期与文稿入口状态同步。
- **文字稿可读性**：深浅主题下当前播放句的文字、时间与背景层级使用专用语义色，提高长文稿阅读辨识度。

## 使用与数据边界

- 本地 ASR 仅在用户明确点击后开始，不会因为浏览、播放、收藏或加入队列而自动准备音频。
- 未部署本地转写模型，或请求本集总结但联网 AI 未通过“保存并测试”验证时，应用不会开始临时音频请求。
- 本地转写与模型部署不会调用联网 AI；仅在用户主动生成精修或本集总结时，当前文字稿才会发送到已验证的联网 AI 服务，音频不会上传。

## 平台与已知限制

- 本地 ASR 与模型部署的发布验证范围为 **Windows x64**。SenseVoiceSmall 模型约 240 MB，安装包不内置模型，需在设置页手动部署或选择本地目录。
- DeepSeek 已完成实机配置与使用验证。其他预设和自定义服务提供 OpenAI Chat Completions 协议层兼容，本版不宣称所有模型、认证方式或 JSON mode 都已完成实机质量认证。
- Portable 与 NSIS 安装包当前未做 Authenticode 签名，首次运行可能出现 SmartScreen 提示。
- Electron 主窗口仍使用 `nodeIntegration: true` 与 `contextIsolation: false`；这是后续独立架构加固项。本版未引入新的不可信远端 HTML 渲染入口。

## 第三方许可

本版没有新增第三方依赖。SenseVoiceSmall、Silero VAD、sherpa-onnx 和 FFmpeg 分别适用其自身许可证与归属要求，详见 [THIRD_PARTY_NOTICES.md](./THIRD_PARTY_NOTICES.md)。
